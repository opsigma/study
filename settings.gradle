//pluginManagement {
//    resolutionStrategy {
//        eachPlugin {
//            if (requested.id.id == "org.hibernate.orm") {
//                useModule("org.hibernate:hibernate-gradle-plugin:${requested.version}")
//            }
//        }
//    }
//}

dependencyResolutionManagement {
    repositories {
        mavenLocal()
        mavenCentral()
    }
}

buildCache {
    local {
        directory = "/.gradle/build-cache"
    }
}

rootProject.name = 'Study'
include 'spring-01', 'spring-01:spring-01-exercise', 'spring-01:spring-01-solution'


